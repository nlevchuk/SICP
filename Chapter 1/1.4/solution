Для примера используем такой вызов:

```
(a-plus-abs-b 2 3)
```

Так как мы используем подстановочную модель вычислений, то нужно подставить аргументы на место операндов и вычислить все составные выражения. 
Это будет выглядеть следующим образом:

```
1. ((if (> 3 0) + -) 2 3)
2. ((if (#t) + -) 2 3)
3. (+ 2 3)
```

- В пункте 1 подставляются аргументы на место операндов.
- В пункте 2 вычисляется значение условного выражения. Так как 3 > 0, то комбинация `(> 3 0)` трансформируется в значение #t (true), следовательно,
возвращается следствие этого условного выражения, которое имеет значение `+`
- В пункте 3 так как все составные выражения вычислены, то вычисляем элементарные выражения. Ответ: 2 + 3 = 5.
