1. Поведение при аппликативном порядке:

При вызове процедуры test, 

  a) Вычисление выражений 0 и (p), которые являются аргументами функции
  b) Применение функции
  c) В теле процедуры будут подставлены вычисленные значения аргументов на место формальных параметров. 
  d) Выполнение выражения if в котором происходит проверка предиката
  e) Так как выражение-предикат вычислиться в #t, то будет возвращено 0 как результат вычисления процедуры.

2. Поведение при нормальном порядке:

При вызове процедуры test,

  a) Применение функции
  b) В теле процедуры будут подставлены, но не вычислены, аргументы на место формальных параметров. 
  c) Выполнение выражения if в котором происходит проверка предиката
  d) Так как выражение-предикат вычислиться в #t, то будет возвращено 0 как результат вычисления процедуры.

Так как в теле процедуры условие if в обоих случаях возвращает выражение-следствие, то нет нужны вычислять альтернативу. При аппликативном порядке вычислений вызов процедуры (p) будет выполнен независимо от условия if (см. шаг а) в пункте 1).

Таким образом, в данном примере, при нормальном порядке вычислений мы экономим один шаг в вычислениях.
